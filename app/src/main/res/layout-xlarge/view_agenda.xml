<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android">

    <View
        android:id="@+id/view_shadow"
        android:layout_width="match_parent"
        android:layout_height="5dp"
        android:background="@drawable/shadow">
    </View>
<!-- no success here
 adds space to the bottam of everthing
 -->
    <com.example.ayush.fire3.agenda.AgendaListView

        android:id="@+id/agenda_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:animationCache="false"
        android:divider="@color/calendar_divider_color"
        android:dividerHeight="1dp"
        android:overScrollMode="never"
        android:scrollbars="none"
        android:scrollingCache="false"/>
    <com.example.ayush.fire3.widgets.FloatingActionButton
        android:id="@+id/floating_action_button"
        style="@style/fab"/>

</merge>